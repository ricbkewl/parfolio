name: Archived ATG foundation migration (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical migration disabled. ParFolio and ATG must remain isolated."
