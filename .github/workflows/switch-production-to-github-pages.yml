name: Retired - GitHub Pages production switch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow
        run: echo "GitHub Pages is redirect-only. Vercel is the sole ParFolio production host."
