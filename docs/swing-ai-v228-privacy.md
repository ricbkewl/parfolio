# Swing video privacy

Test swing videos are not committed to GitHub. Current browser preview uses a local object URL for selected media. Future persistence must use ParFolio-only private storage, authenticated ownership/RLS, signed access, deletion controls and explicit retention rules. ATG private storage/data must never be used.