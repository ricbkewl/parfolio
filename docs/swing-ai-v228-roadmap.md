# Swing AI automatic detector roadmap

Current v228: automatic-first phase proposals plus manual correction fallback.

Next detector layers: pose/body landmarks → phase classifier using pose motion → golf-specific club/clubhead tracking → swing plane/path → confidence-aware analytics → AI Coach from verified structured measurements.

No launch-monitor metrics are inferred from ordinary phone video.