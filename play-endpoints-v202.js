/* ParFolio — endpoint marker layer retired.
   TEE and CENTER flags/labels are intentionally no longer rendered in round view.
   Route lines, planner targets, yardages, and map-provider behavior remain unchanged. */
(function(){
  // Intentionally empty. Kept as a compatibility shim because index.html may still load this file.
})();
